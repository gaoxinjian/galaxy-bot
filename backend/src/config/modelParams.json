{
  "common": {
    "temperature": {
      "type": "float",
      "default": 0.7,
      "min": 0.0,
      "max": 2.0,
      "step": 0.1,
      "description": "控制输出的随机性，值越高创造性越强"
    },
    "top_p": {
      "type": "float",
      "default": 0.9,
      "min": 0.0,
      "max": 1.0,
      "step": 0.01,
      "description": "核采样阈值，控制输出多样性"
    },
    "top_k": {
      "type": "int",
      "default": 40,
      "min": 1,
      "max": 100,
      "step": 1,
      "description": "限制候选 token 数量"
    },
    "max_tokens": {
      "type": "int",
      "default": 2048,
      "min": 1,
      "max": 4096,
      "step": 1,
      "description": "最大生成 token 数"
    },
    "think": {
      "type": "int",
      "default": 1,
      "min": 0,
      "max": 1,
      "step": 1,
      "description": "是否显示思考过程 (0=否, 1=是)"
    }
  },
  "models": {
    "qwen:9b": {
      "displayName": "Qwen 3.5 9B (快速)",
      "parameters": {
        "temperature": { "default": 0.7 },
        "top_p": { "default": 0.9 },
        "top_k": { "default": 40 }
      }
    },
    "qwen:35b-a3b": {
      "displayName": "Qwen 3.6 35B-A3B (精准)",
      "parameters": {
        "temperature": { "default": 0.6 },
        "top_p": { "default": 0.95 },
        "top_k": { "default": 50 }
      }
    },
    "llama3.2": {
      "displayName": "Llama 3.2",
      "parameters": {
        "temperature": { "default": 0.8 }
      }
    }
  }
}
